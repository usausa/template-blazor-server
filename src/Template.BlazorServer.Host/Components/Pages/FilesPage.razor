@page "/files"
@attribute [Authorize]

<PageTitle>Files</PageTitle>

<MudText Typo="Typo.h4" Class="mb-4">Files</MudText>

<MudStack Row="true" AlignItems="AlignItems.Center" Class="mb-4">
    <MudFileUpload T="IBrowserFile" FilesChanged="UploadAsync" Accept="*">
        <CustomContent>
            <MudButton Variant="Variant.Filled" Color="Color.Primary" StartIcon="@Icons.Material.Filled.Upload" OnClick="@context.OpenFilePickerAsync">アップロード</MudButton>
        </CustomContent>
    </MudFileUpload>
</MudStack>

<Condition Value="uploading">
    <MudProgressLinear Value="progress" Color="Color.Primary" Class="upload-progress mb-4" />
</Condition>

<MudSimpleTable Dense="true" Class="files-table">
    <thead>
        <tr>
            <th>ファイル名</th>
            <th class="w-120">操作</th>
        </tr>
    </thead>
    <tbody>
        <ListItem Items="entries" Context="entry">
            <ItemTemplate>
                <tr>
                    <td>@entry</td>
                    <td>
                        <MudIconButton Icon="@Icons.Material.Filled.Download" Size="Size.Small" Href="@MakeDownloadUrl(entry)" />
                        <AuthorizeView Roles="@Roles.Administrator" Context="auth">
                            <MudIconButton Icon="@Icons.Material.Filled.Delete" Size="Size.Small" Color="Color.Error" OnClick="() => DeleteAsync(entry)" />
                        </AuthorizeView>
                    </td>
                </tr>
            </ItemTemplate>
        </ListItem>
    </tbody>
</MudSimpleTable>
